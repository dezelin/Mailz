'use strict';

/* global define */

define(
  [
    'app'
  ],

  function(app) {
    app.register.controller('NavbarController',
      [
        '$scope',

        function($scope) {

        }]);
  });
